export interface IOption {
    orientation: string;
    padding: number;
    stylesheet: string;
    output: string;
    custom_styles: any;
    path_prefix: string;
    enable_cache_busting: boolean;
}