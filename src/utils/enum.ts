export enum RightClickType {
    Folder,
    File
}

export enum Stylesheet {
    Css = "css",
    Scss = "scss",
    Less = "less"
}

export enum Orientation {
    Vertical = "vertical",
    Horizontal = "horizontal"
}